{{input
  type='date'
  value=newFromDate
  class='form-control'
}}
{{input
  type='date'
  value=newToDate
  class='form-control'
}}
{{input
  type='text'
	placeholder='Search...'
  value=filter
  class='form-control'
}}
{{#each rangeFilteredContent as |fact|}}
  <li>
	  {{fact.title}}	
	  |
		{{fact.description}}
		|
		{{ago fact.start_date}}

  </li>
{{/each}}