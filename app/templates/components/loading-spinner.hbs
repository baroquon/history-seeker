<svg class="circular" style='width: {{cssParentSize}}; height: {{cssParentSize}}'>
  <circle class="path" cx="{{cx}}" cy="{{cy}}" r="{{radius}}" fill="none" stroke-width="4" stroke-miterlimit="10" style='width: {{cssSize}}; height: {{cssSize}}'/>
</svg>
