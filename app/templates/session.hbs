{{#pre-auth-wrapper}}
  {{partial '_pre-auth-back-header'}}
  <section class="content-section single-content text-center">
    <div class="login-section">
      <div class="container">
        <div class="row">
          <div class="col-md-6 col-md-offset-3">
            {{partial '_auth-nav'}}
            {{liquid-outlet}}
          </div>
        </div>
      </div>
    </div>
  </section>
{{/pre-auth-wrapper}}
